<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        /* Print-optimized styles */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @media print {
            @page {
                margin: 15mm;
                size: A4;
            }

            body {
                font-size: 12pt;
                line-height: 1.4;
            }

            .no-print {
                display: none !important;
            }

            .page-break {
                page-break-before: always;
            }

            table {
                page-break-inside: avoid;
            }

            tr {
                page-break-inside: avoid;
            }
        }

        @media screen {
            body {
                font-size: 14px;
                line-height: 1.5;
                background: #f5f5f5;
                padding: 20px;
            }

            .container {
                max-width: 800px;
                margin: 0 auto;
                background: white;
                padding: 40px;
                box-shadow: 0 0 20px rgba(0,0,0,0.1);
                border-radius: 8px;
            }
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #333;
        }

        .header {
            text-align: center;
            border-bottom: 3px solid #2563eb;
            padding-bottom: 20px;
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 22px;
            color: #1e40af;
            margin-bottom: 5px;
        }

        .header h2 {
            font-size: 20px;
            color: #475569;
            font-weight: normal;
        }

        .header .subtitle {
            font-size: 14px;
            color: #64748b;
            margin-top: 10px;
        }

        .employee-section {
            background: #f8fafc;
            border: 1px solid #e2e8f0;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 30px;
        }

        .employee-section h3 {
            color: #1e40af;
            font-size: 18px;
            margin-bottom: 15px;
            border-bottom: 1px solid #cbd5e1;
            padding-bottom: 5px;
        }

        .employee-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }

        .employee-item {
            display: flex;
            flex-direction: column;
        }

        .employee-item label {
            font-weight: 600;
            color: #374151;
            font-size: 12px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .employee-item span {
            font-size: 16px;
            color: #111827;
            margin-top: 3px;
        }

        .stats-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: linear-gradient(135deg, #2563eb, #3b82f6);
            color: white;
            padding: 20px;
            border-radius: 8px;
            text-align: center;
        }

        .stat-card h4 {
            font-size: 14px;
            opacity: 0.9;
            margin-bottom: 8px;
            font-weight: normal;
        }

        .stat-card .value {
            font-size: 24px;
            font-weight: bold;
        }

        .assets-section h3 {
            color: #1e40af;
            font-size: 18px;
            margin-bottom: 20px;
            padding-bottom: 5px;
            border-bottom: 2px solid #e2e8f0;
        }

        .assets-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        .assets-table th {
            background: #1e40af;
            color: white;
            font-weight: 600;
            padding: 12px 8px;
            text-align: left;
            font-size: 14px;
        }

        .assets-table td {
            padding: 10px 8px;
            border-bottom: 1px solid #e2e8f0;
            vertical-align: top;
        }

        .assets-table tbody tr:nth-child(even) {
            background: #f8fafc;
        }

        .assets-table tbody tr:hover {
            background: #e2e8f0;
        }

        .no-assets {
            text-align: center;
            color: #6b7280;
            font-style: italic;
            padding: 40px 20px;
            background: #f9fafb;
            border: 2px dashed #d1d5db;
            border-radius: 8px;
        }

        .footer {
            margin-top: 40px;
            text-align: center;
            font-size: 12px;
            color: #6b7280;
            border-top: 1px solid #e2e8f0;
            padding-top: 20px;
        }

        /* Print controls */
        .print-controls {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            background: white;
            padding: 15px;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            border: 1px solid #e2e8f0;
        }

        .print-btn {
            background: #2563eb;
            color: white;
            border: none;
            padding: 12px 20px;
            border-radius: 6px;
            cursor: pointer;
            font-size: 14px;
            font-weight: 600;
            margin: 0 5px;
            transition: all 0.2s;
        }

        .print-btn:hover {
            background: #1d4ed8;
            transform: translateY(-1px);
        }

        .print-btn.secondary {
            background: #6b7280;
        }

        .print-btn.secondary:hover {
            background: #4b5563;
        }

        .print-instructions {
            background: #fef3c7;
            border: 1px solid #f59e0b;
            color: #92400e;
            padding: 15px;
            border-radius: 6px;
            margin-bottom: 20px;
            font-size: 14px;
        }

        .print-instructions strong {
            color: #78350f;
        }
    </style>
</head>
<body>
    <!-- Print Controls (hidden when printing) -->
    <div class="print-controls no-print">
        <button class="print-btn" onclick="window.print()">
            🖨️ Print / Save as PDF
        </button>
        <button class="print-btn secondary" onclick="window.history.back()">
            ← Back
        </button>
    </div>

    <div class="container">
        <!-- Print Instructions (hidden when printing) -->
        <div class="print-instructions no-print">
            <strong>📋 How to save as PDF:</strong> Press Ctrl+P (or Cmd+P on Mac), then select "Save as PDF" as your printer destination.
        </div>

        <!-- Header -->
        <div class="header">
            <h1>IT Asset Report</h1>
            <h2><%= employee.name %> (<%= employee.cep %>)</h2>
            <div class="subtitle">Generated on <%= new Date().toLocaleDateString('en-US', {
                weekday: 'long',
                year: 'numeric',
                month: 'long',
                day: 'numeric'
            }) %></div>
        </div>

        <!-- Employee Information -->
        <div class="employee-section">
            <h3>👤 Employee Information</h3>
            <div class="employee-grid">
                <div class="employee-item">
                    <label>Full Name</label>
                    <span><%= employee.name %></span>
                </div>
                <div class="employee-item">
                    <label>CEXPT ID</label>
                    <span><%= employee.cep %></span>
                </div>
                <div class="employee-item">
                    <label>Email Address</label>
                    <span><%= employee.email || 'Not specified' %></span>
                </div>
                <div class="employee-item">
                    <label>Department</label>
                    <span><%= employee.department_name || 'Not assigned' %></span>
                </div>
                <div class="employee-item">
                    <label>Location</label>
                    <span><%= employee.location_name || 'Not specified' %></span>
                </div>
                <div class="employee-item">
                    <label>Employee Since</label>
                    <span><%= employee.created_at ? new Date(employee.created_at).toLocaleDateString() : 'Unknown' %></span>
                </div>
            </div>
        </div>

        <!-- Assets List -->
        <div class="assets-section">
            <h3>📦 Assigned Assets - Total Assets: <%= stats.total_items %></h3>

            <% if (items && items.length > 0) { %>
                <table class="assets-table">
                    <thead>
                        <tr>
                            <th>CEXPT ID</th>
                            <th>Asset Name</th>
                            <th>Type</th>
                            <th>Serial Number</th>
                            <th>Date Assigned</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% items.forEach((item, index) => { %>
                            <tr>
                                <td><%= item.cep_brc || 'N/A' %></td>
                                <td><strong><%= item.name || 'Unnamed Asset' %></strong></td>
                                <td><%= item.type_name || 'Unknown' %></td>
                                <td><%= item.serial_cod || 'N/A' %></td>
                                <td><%= item.date_assigned ? new Date(item.date_assigned).toLocaleDateString('en-US', {year: 'numeric', month: 'short', day: 'numeric'}) : 'N/A' %></td>
                            </tr>
                        <% }); %>
                    </tbody>
                </table>



            <% } else { %>
                <div class="no-assets">
                    <h4>No Assets Assigned</h4>
                    <p>This employee currently has no assets assigned to them.</p>
                </div>
            <% } %>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div>IT Asset Manager | Report generated on <%= new Date().toLocaleString() %></div>
            <div>Employee: <%= employee.name %> (<%= employee.cep %>) | Total Assets: <%= stats.total_items %></div>
        </div>
    </div>

    <script>
        // Auto-focus print dialog shortcut
        document.addEventListener('keydown', function(e) {
            if (e.ctrlKey && e.key === 'p') {
                e.preventDefault();
                window.print();
            }
        });

        // Show print dialog on load if requested
        if (window.location.search.includes('autoprint=true')) {
            window.addEventListener('load', function() {
                setTimeout(() => window.print(), 500);
            });
        }

        // Add print timestamp
        window.addEventListener('beforeprint', function() {
            console.log('PDF print initiated for employee:', '<%= employee.name %>');
        });
    </script>
</body>
</html>


